"
I am the exception raised if the end square is reached a second time, which implies there are multiple possible solutions - so it's not a valid Sudoku!

"
Class {
	#name : #SudokuNotUnique,
	#superclass : #Error,
	#category : #'Sudoku-Kernel'
}
